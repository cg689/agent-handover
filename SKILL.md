---
name: agent-handover
description: >-
  Agent 项目交接技能。当用户要求进行项目交接、更换 AI 编程助手（Agent）、
  或需要生成交接文档（HANDOVER.md / 项目说明书）时使用此技能。
  涵盖交接收尾、隐性知识显性化、移交配置、新 agent 验证及生命周期归档的完整流程。
---

# Agent 项目交接技能

本技能指导你完成 AI 编程助手（Agent）项目交接的全流程。

## 核心原则

1. **项目不属于任何 agent**，项目属于它的存储位置（本地磁盘或远程仓库）。换 agent = 换工人，不是搬项目。
2. **只有写进文件的知识能带走**。会话记忆、长期记忆、项目索引缓存——换 agent 全部丢失。
3. **交接 = 隐性知识显性化 + 验证新 agent 没接错 + 生命周期归档闭环。**

---

## ⚡ 第 0 步：判断你的角色和场景（必做）

当用户提出交接相关需求时，你需要先确定两件事：

### A. 判断你是旧 agent 还是新 agent

按以下逻辑综合判断你的角色：

1. **优先遵循用户指令中的明确意图**：
   - 用户指令包含"发起交接 / 准备交接 / 换到新 agent / 写交接文档" → 你是**旧 agent（交出方）**。
   - 用户指令包含"刚切换过来 / 刚接手 / 阅读交接文档入职 / 完成入职验证" → 你是**新 agent（接收方）**。
2. **若指令较模糊（如仅说"帮我做项目交接"），检查根目录 `HANDOVER.md` 状态**：
   - **不存在 `HANDOVER.md`** → 你是**旧 agent（交出方）**。
   - **存在 `HANDOVER.md`**：检查文档头部的元信息与 Git 提交记录：
     - 若状态为 `PENDING`（待接收）且为最近提交 → 你是**新 agent（接收方）**。
     - 若状态为 `COMPLETED`（已完成）或该文档生成后已有多次后续业务提交 → 这是上次的历史交接遗留，当前你是**旧 agent（交出方）**。
3. **依然存疑时** → **主动向用户确认**：
   > "检测到项目中存在历史交接文档。请问您本次是想**发起新的交接（交出给新 Agent）**，还是**刚接手该项目（新 Agent 入职）**？"

### B. 判断完整版还是轻量版

如果你是旧 agent，**主动问用户**：

> "你是要**长期切换**到新 Agent（我之后会停用），还是**临时试用**一下新 Agent（我继续保留）？"

- 用户说长期切换 / 停用旧 agent → **完整流程**
- 用户说临时试用 / 两边都保留 → **轻量版流程**
- 用户没说清楚 → 默认按**完整流程**，更安全

---

## 🔀 旧 agent 工作流（交出方）

你负责第 1-2 步。做完后告诉用户去新 agent 完成剩余步骤。

### 轻量版（临时试用）

1. 确认 Git 状态（无遗留未忽略垃圾），提交所有改动到 git
2. 写精简版 HANDOVER.md（包含顶部元信息：`交接状态: PENDING`）
   → 使用模板：[handover_prompt.md](./resources/handover_prompt.md)
3. 告诉用户：
   > "收尾完成。请在新 agent 中打开这个项目文件夹，然后对新 agent 说：
   > **'我刚从其他 Agent 切换过来，请阅读 HANDOVER.md 完成入职验证。'**
   > ⚠️ 试用期结束后若决定不切换（继续留在旧 Agent），请务必删除根目录的 `HANDOVER.md`，避免残留脏状态阻碍未来交接。"

### 完整版（长期切换）

#### 第 1 步：收尾

目的：项目处于干净、完整、可交接状态。

操作：
1. 确认当前 Git 分支名称（`git branch --show-current`）及无遗留暂存（`git stash list`）。
2. 核对 `git status` 确保临时文件/构建产物已被忽略后，将改动全部提交到 git（半成品标注"进行中"）。
3. 打带时间戳的回滚点：`git tag handover-before-$(date +%Y%m%d%H%M)`（避免多次交接时 Tag 重名报错）。
4. 运行测试/构建，记录结果（区分历史遗留与新引入的失败）。
5. 有远程仓库则推送（含 tag）。

→ 详细提示词模板：[step1_cleanup_prompt.md](./resources/step1_cleanup_prompt.md)

#### 第 2 步：显性化（最易遗漏）

> ⚠️ 顺序有讲究：
> 1. **必须先完成第 1 步再写文档**，否则"当前状态"会失真。
> 2. **若根目录已有历史 `HANDOVER.md`**，必须先将其备份归档至 `docs/handover/`，**绝不可直接原地覆写**，避免丢失历史决策与踩坑记录。

生成两份文档：

**① HANDOVER.md**（快照——项目现在怎么样）
顶部必须包含状态头：
```markdown
---
交接状态: PENDING
交接时间: YYYY-MM-DD
交出方: [当前 Agent 名称]
接收方: [目标 Agent 名称 / 待定]
---
```
→ 提示词模板：[step2_handover_prompt.md](./resources/step2_handover_prompt.md)

**② 项目说明书**（长效——干活守什么规矩）
→ 内容模板：[project_guide_template.md](./resources/project_guide_template.md)

文件名按目标 agent 约定（对照表见 [完整指南](./references/full_guide.md)）。
如果用户说不清新 agent 用什么文件名，先写成通用名 `PROJECT_GUIDE.md`，到新 agent 再改名。

两份文档联动：HANDOVER.md 中稳定下来的内容回填到项目说明书。

#### 完成后，告诉用户：

> "交接文档已就绪。接下来请完成以下操作：
> 1. 在新 Agent 中**打开同一个项目文件夹**
> 2. 迁移项目必需的配置（.env、密钥等——文档里已记录位置）
> 3. 如有 MCP/工具链配置，在新 Agent 中重新配置
> 4. 对新 Agent 说：**'我刚从其他 Agent 切换过来，请阅读 HANDOVER.md 和项目说明书，完成入职验证。'**
>
> 我进入只读顾问期——如果新 Agent 有疑问，可以回来问我，但我不再改文件。"

---

## 🆕 新 agent 工作流（接收方）

你负责第 3-4 步。项目文件夹中应已存在 HANDOVER.md（旧 agent 留下的）。

### 第 3 步：环境确认

检查清单：
- [ ] 找到并阅读 HANDOVER.md（确认其交接状态为 PENDING）
- [ ] 找到并阅读项目说明书（CLAUDE.md / AGENTS.md / GEMINI.md 等，按你的约定）
- [ ] 如果项目说明书文件名不符合你的约定，建议用户重命名或复制一份
- [ ] 确认 .env / 密钥 / 本地数据库等配置已就位（如未就位，告诉用户缺什么）
- [ ] 确认远程仓库访问正常

### 第 4 步：验证与生命周期闭环

**阶段 A：只读入职**

全程只读、不修改任何文件：
1. 阅读 HANDOVER.md 和项目说明书
2. 浏览目录结构，说明你对项目架构的理解
3. 运行安装依赖、测试、构建的标准命令，确认项目可用
4. 汇报：项目现状、发现的问题或与交接文档不符的地方

→ 详细提示词模板：[step4_onboarding_prompt.md](./resources/step4_onboarding_prompt.md)

向用户汇报后等待确认，说错了接受纠正，说对了才继续。

**阶段 B：受控小任务与人类审查（防 AI 自测作弊）**

请用户指定一个范围明确、结果可验证的小改动（修小 bug、补测试、加字段）。
执行时：
- 严格按项目说明书的约定动手（提交规范、跑测试）
- 不碰范围外的东西
- **完成改动后生成 `git diff` 供人类审核**，严防擅自修改测试断言以制造虚假全绿。

**阶段 C：交接闭环与归档（关键！）**

人类审核通过后，**必须完成归档闭环**，避免阻碍未来下一次交接：
1. 将 `HANDOVER.md` 中稳定下来的技术事实（目录结构、环境配置、常用命令等）**回填沉淀到项目说明书**（如 `AGENTS.md` / `CLAUDE.md` 等）。
2. **将 `HANDOVER.md` 归档移走**：先确保归档目录存在（如 `mkdir -p docs/handover`），再移至 `docs/handover/HANDOVER_YYYYMMDD_<交出方>_to_<接收方>.md`（或将头部状态更新为 `COMPLETED`）。
3. 提交本次入职验证及归档修改到 git。

完成后告诉用户：
> "入职验证与归档闭环已完成，我已完全理解项目现状并通过受控任务验证。可以开始正式接受开发任务了。
> 建议：旧 Agent 的配置目录现在可以清理了；后续重要决策请继续写入项目说明书。"

---

## 红线

1. 新旧 agent 不要同时改文件
2. 别假设新 agent "应该知道"任何事——它只知道文件里写的
3. 交接文档中**禁止明文密钥、Token、私网 IP 或真实业务测试数据**
4. 没有回滚点不要开始交接（必须使用带时间戳的 tag）
5. 不要跳过验证与人类 Diff 审计步骤
6. **交接完成后必须归档或标记 HANDOVER.md**，绝不可遗留未闭环的活跃交接状态

---

## 回滚与应急

- 回退：`git checkout <handover-before-tag名称>`
- 保留旧 agent 1-2 周只读顾问期
- 发现遗漏：回旧 agent 写成文档再带回来，不要凭记忆硬补

---

## 参考资料

- [全真演练指南](./references/QUICK_START.md)：手把手带你看 Antigravity、Cursor 等真实场景对话演练与急救手册
- [完整指南](./references/full_guide.md)：详细的交接通用指南全文（含资产清单、Agent 对照表、特殊场景、自检清单等）
